# bart-mnli-zero-shot-classification-pipeline

DIMER pipeline scaffold for **facebook/bart-large-mnli** — Zero-shot text classification via NLI entailment.

| | |
|---|---|
| Upstream model | [`facebook/bart-large-mnli`](https://huggingface.co/facebook/bart-large-mnli) |
| Pinned revision | `d7645e127eaf1aefc7862fd59a17a5aa8558b8ce` (resolved 2026-09-13) |
| Upstream license | `mit` (verified on the Hub 2026-09-13; re-check at the pinned revision before release) |
| Weight files to stage | `model.safetensors` |
| Status | scaffold only — no weights downloaded, no pipeline code yet |

Weights are staged under `weights/` and are git-ignored. This repository follows the
MODEL_CARD_SPEC 1.1 / NOTEBOOK_SPEC 1.1 conventions used by the other `*-pipeline` repos.
