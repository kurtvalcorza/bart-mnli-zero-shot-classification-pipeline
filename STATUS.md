# Release status

Current status: **Candidate** — E2E fine-tuning tutorial source complete, awaiting supported clean-runtime execution. The pipeline package (inference contract, digest-pinned Banking77 corpus, accuracy/macro-F1 with the majority baseline, bounded decoder-and-head fine-tuning on NLI pairs, safetensors adapter), 35 offline unit tests plus 5 notebook-parity tests and 2 model-backed tests, one executed local CPU run of the committed notebook blob, the MODEL_CARD_SPEC 1.1 `MODEL_CARD.md`, and the standalone `E2E` tutorial `tutorials/bart_zero_shot_classification_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). Unit tests and static conformance checks run in GitHub Actions. The tutorial remains non-release-grade until clean-runtime execution evidence from a supported hosted runtime for the exact candidate commit is recorded according to `docs/release-verification.md`.

The earlier `TASK-INFERENCE` notebook had a recorded Kaggle CPU pass (blob `f8a761ce5419` at `34098a7`, 2026-09-14); that evidence is retained in `docs/release-verification.md` as history and does not carry over to the `E2E` notebook, which is a different blob with a different default path.

No licence or remote-code gate is open for DIMER hosting (MIT weights; safetensors; `trust_remote_code=False`). The adaptation sample is fetched at run time from the pinned `PolyAI-LDN/task-specific-datasets` commit (Banking77, CC BY 4.0, two CSV files, 1.1 MB) and is not redistributed by this repository. Two behaviours worth a reviewer's eye: the adapted model remains an NLI scorer over any label set but has been pulled towards ten phrases and one template, so its zero-shot behaviour elsewhere is changed and unmeasured; and the scores stay uncalibrated after adaptation — the card, notebook and README say so.
